version: '3.8'

services:
  postgres_database:
    image: postgres:16
    container_name: postgres_database
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    networks:
      - db_network


networks:
  db_network:
    driver: bridge
